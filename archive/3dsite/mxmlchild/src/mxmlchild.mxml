<?xml version="1.0" encoding="utf-8"?>
<s:Application xmlns:fx="http://ns.adobe.com/mxml/2009"
			   xmlns:s="library://ns.adobe.com/flex/spark"
			   xmlns:mx="library://ns.adobe.com/flex/mx"
			   width="500" height="500" minWidth="500" minHeight="500" applicationComplete="init();"
			   backgroundAlpha="0.0" backgroundColor="#FFFFFF" preloaderChromeColor="#CCCCCC"
			   resize="resizeHandler(event);">
	<fx:Style source="msxmlchild.css"/>
	<fx:Declarations>
		<!-- Place non-visual elements (e.g., services, value objects) here -->
	</fx:Declarations>
	<!-- <fx:Script source="test.as" /> -->
	
	<fx:Script>
		<![CDATA[
			//import classtest;
			//private var mc:classtest = new classtest;
			
			
			import mx.events.ResizeEvent; 
			private function resizeHandler(event:ResizeEvent):void
			{ 
				/*
				if (!stage) return;
				
				trace("height:" + stage.height); 
				trace("height:" + stage.width); 
				
				// center the view to the middle of the stage
				var w:int = stage.stageWidth;
				var h:int = stage.stageHeight;
				
				// relocate the 2d page
				var x:int = w/2 - this.width/2;
				var y:int = h/2 - this.height/2;
				this.x = x;
				this.y = y;
				*/
			} 
			
			import mx.containers.HBox;
			import mx.containers.VBox;
			import mx.core.UIComponent; 
			private var bgColor:uint = 0xFFCC00; 
			private var hbox:HBox = null;
			
			private function none():void
			{
				//hbox = new HBox();	
				//var myclass:MyClass = new MyClass();
				//hbox.addChild(myclass);
				//addChild(hbox);
			}
			
			private function init():void
			{
				/*
				var ref : UIComponent = new UIComponent();
				
				
				this.addElement( ref );
				
				//var a:away36 = new away36();
				var a:mixed = new mixed();	
				ref.addChild( a );
				*/
			}
			
			
		]]>
	</fx:Script>
	<s:Image x="10" y="21" width="155" height="131" source="@Embed('../assets/djc_alpha.png')"/>
	<s:Image x="83" y="145" width="231" height="174" source="@Embed('../assets/djc_alpha.png')"/>
	<s:Image x="227" y="277" width="313" height="213" source="@Embed('../assets/djc_alpha.png')"/>
	<s:Label x="17" y="455" width="129" height="46" fontSize="35" text="STUFF"/>
	<s:Label x="361" y="10" width="129" height="46" fontSize="35" text="STUFF"/>
	
	
</s:Application>

